package javaprogressivo.pooparteiii.listainterface.exemplos.domain;

public interface Matriz {
    double Exibir(Matriz m);
    Matriz Anular(int linha, int coluna);
    Matriz AdicionarAsterisco(Matriz m);
}
