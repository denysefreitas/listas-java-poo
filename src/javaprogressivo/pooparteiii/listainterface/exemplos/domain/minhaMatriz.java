package javaprogressivo.pooparteiii.listainterface.exemplos.domain;

public class minhaMatriz implements Matriz{
    Matriz matriz;
    double traco;

    @Override
    public double Exibir(Matriz m) {
        // Exibir matriz
        return traco;
    }

    @Override
    public Matriz Anular(int linha, int coluna) {
        return matriz;
    }

    @Override
    public Matriz AdicionarAsterisco(Matriz m) {
        return matriz;
    }
}
